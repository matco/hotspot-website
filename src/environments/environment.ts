//the build system defaults to the dev environment which uses "environment.ts"
//when specifying "ng build --env=prod", then "environment.prod.ts" is used instead

export const environment = {
	production: false
};
